# Pulse impl log

Running progress record. Newest entries at the bottom. Keep entries short: what happened, what's next, blockers.

Purpose is context preservation across sessions — decisions, gotchas, and dead ends that the code and `git log` do not explain on their own. Stage plans live in the numbered impl notes; stage order lives in [`ROADMAP.md`](ROADMAP.md). This file records what actually happened.

Entries before 2026-07-26 were reconstructed from `git log` and the archived impl notes rather than written live, so they are thinner than later ones.

## 2026-06-07 — Scaffold

- Initial repo: Tauri 2 + React shell alongside a standalone Rust engine crate. MIT, open-source first.

## 2026-06-13 — Stages 1–2: engine validation and HAL format checks

- `pulse-cli` created as the engine harness (#1): file probing, device listing, format inspection.
- HAL format validation (#2): hog mode via `kAudioDevicePropertyHogMode`, nominal sample-rate switching, physical-format probing through `objc2-core-audio` directly.

## 2026-06-14 — Stages 3–5: AUHAL pivot, CLI config, design-first reorder

- **Playback moved from raw HAL IOProc to AUHAL.** Raw IOProc hit a client-format mismatch on the Matrix hardware — the physical stream could be integer while the callback buffer stayed float32, and writing integer bytes into it produced heavy noise. AUHAL makes the contract explicit: Pulse feeds interleaved float32 and Core Audio converts.
- Consequence carried forward: **no hard bit-perfect claims for this path.** The honest claim is native-rate exclusive playback with no Pulse-side DSP. Raw integer HAL is parked as a future experiment.
- Stage 4: UID-backed CLI default output device config, so harness runs are repeatable. Device identity is persisted by UID, never by transient `AudioDeviceID`.
- Stage 5 reordered mid-flight: a backend device-settings stage was replaced by design-first (#5), on the rule that no frontend surface gets built before its Pencil design exists.
- First Pencil mockup: cyberpunk library screen.

## 2026-06-15 — Design: tracks and playlists

- Added Tracks and Playlists library pages to `design/pulse-desktop.pen`.

## 2026-06-16 — Stage 6 scoped

- `0006-playback-controller.md` written: UI-agnostic controller inside `pulse-engine`, command/event API, conservative pause/resume, seek as controlled restart. Queue commands explicitly deferred until single-file transport is stable.

## 2026-07-20 — GPUI pivot (impl 0007)

- **Tauri 2 + React dropped for a native GPUI app.** Trigger: Runner's cancelled GPUI rewrite had de-risked the framework end to end (production rendering, Chinese IME via `EntityInputHandler` with no platform code, hand-rolled `.app` assembly + Developer ID codesign). That rewrite died to the parity treadmill of replacing 28k LOC of shipped UI; Pulse had zero shipped UI, so the killer risk did not exist here.
- Rationale that stuck: Pulse is macOS-only by nature (Core Audio) and already deep in `objc2-*`, so GPUI's macOS-first Metal posture costs nothing, and the UI can observe the engine in-process — no IPC, no serialization, no webview frame pacing.
- Deleted `src/`, `src-tauri/`, and the whole JS toolchain; added `crates/pulse-app`. Workspace lockfile shed ~4,500 lines of webview dependency tree.
- Accepted risk, still open at the time: the cinematic design language means paint code, not stylesheets.

## 2026-07-21 — Stage 6 implemented

- `PlaybackController` landed in `pulse-engine`: `PlayFile`/`Pause`/`Resume`/`Seek`/`Stop`/`SetOutputDevice`, five events, fake-backend tests for pause/resume/seek and device restart, plus `smoke-pause` and `smoke-seek` CLI commands.

## 2026-07-22 — Makefile

- `make build/run/check/test/clippy/fmt/verify`. `make verify` is the gate: check, test, clippy with `-D warnings`, fmt check.

## 2026-07-26 — Controller hardened

- Codex crew review pass on the stage 6 diff. 17 → 22 tests. New coverage: drop-stops-playback with a live sender clone, play-while-playing backend reuse, paused seek not compounding error, output-device failure stopping playback, end-of-track stop-failure ordering, backend stop failure emitting error instead of paused, pause releasing the backend while seek reuses a resumed one.
- **Still outstanding:** hardware smoke on the Matrix Mini-i Pro 4. Agents cannot verify sound; this needs Jason's ears and has been carried forward since.

## 2026-07-26 — GPUI-CE dependency swap (impl 0008)

- Moved off crates.io `gpui` to rev-pinned [GPUI-CE](https://github.com/gpui-ce/gpui-ce) (`gpui` + `gpui_platform` at `6c799b8e99`).
- Two blockers forced it: crates.io `gpui 0.2.2` (~Nov 2025) is the final publish and predates the platform split, so there is no upgrade path; and `backdrop_filter` exists only in gpui-ce.
- Entry point changed: `Application::new()` no longer exists, construction is `gpui_platform::application()`.
- This reversed an earlier stance ("stay on crates.io until a concrete blocker, then a minimal patch-fork"). The blocker appeared and the whole-fork route won. Enduring rules now live in [`arch/tech-stack.md`](../arch/tech-stack.md); the decision notes are archived.
- Later finding: the `backdrop_filter` justification is **currently unexercised** — the designed surfaces are flat `bg-surface` with borders, no `background_blur` anywhere. The abandoned-crate reason is what carries the swap.

## 2026-07-26 — Asset layer and a palette correction

- **`theme.rs` had the wrong accent for a session.** It was written from a 36-day-old palette memory saying amber `#F5A624`; the actual design file is "Design System · Cyberpunk Neon" with magenta `#FF2D7E`. Caught by reading the `.pen` before building UI on top of it. Lesson recorded: re-read `get_variables` before writing theme code, the `.pen` is ground truth.
- Regenerated all tokens from the live design: 22 colors, 3 radii, 3 font families.
- None of the design's font families (Rajdhani, Inter, Geist Mono) are macOS system fonts, so they are embedded — `crates/pulse-app/assets/` with the four TTFs (SIL OFL) plus the lucide icons the row needs (ISC), behind a hand-rolled `AssetSource` over `include_bytes!`. No `rust-embed` dependency.

## 2026-07-26 — Docs restructured

- Adopted Runner's convention: an `archive/` subdir per docs section; notes move there once their stage ships or their decision is applied, keeping their number. The active listing answers "what is being worked on now".
- Archived stages 1–4, 6, and the applied decision notes 0007/0008. Rule added and immediately exercised: **fold enduring decisions into `docs/arch/` before archiving**, so an archive never buries live guidance.
- Swept stale content: `tech-stack.md` still called the shell crates.io `gpui`, two docs pointed at Zed's GPUI source instead of the gpui-ce checkout (different renderer — reading Zed's would describe a binary we do not run).

## 2026-07-26 — Stage 7: playback row MVP shipped (impl 0009, PR #7)

- Scope narrowed to one runnable slice on Jason's call: drag a file in, it plays, play/pause, drag-to-seek. Built by the codex crew from `0009`.
- Landed: window-wide single-file drop (FLAC, ALAC in M4A, AIFF, WAV) with visible rejection messages, the `qKkw7` row rendered from theme tokens, play/pause on real `PlaybackState`, a 16px drag target around the 4px visual track sending one `Seek` on release, and macOS CI running `make verify`.
- `make verify` green at 32 tests (22 engine, 8 app, 2 CLI). Jason confirmed a dropped file plays and the feel is good.
- **The pivot's headline risk resolved favorably: GPUI matched the layout geometry, icon sizing, badge offsets, 4px track, and static Rajdhani weights without fighting the design.**
- Design decisions taken honestly rather than faked: neutral placeholder instead of the design's cover art (extraction out of scope), real single-track queue count instead of the design's static `7`.
- Tooling limit found: GPUI's offscreen Metal `render_to_image` path omits all text, including system-font controls, so automated pixel comparison of text is not possible. The live window renders text fine.
- Scope creep to note: CI was added beyond the mission brief. Kept — it is good — but it was not asked for.

## 2026-07-26 — Stages 7.5 and 8 planned (impls 0010, 0011)

- Read the full design set to plan the next milestone. Wrote `0010-app-shell.md` and `0011-output-device-management.md`.
- **The app shell is the real prerequisite.** Device management and Storage both live inside chrome that does not exist — today the app is a bare row in an empty window. The chrome is identical across all four screen frames and has no design gaps, so it goes first as stage 7.5.
- Findings from the design read, each recorded in the notes:
  - The shipped row was built from the standalone `qKkw7` component, but every screen instantiates it with a **52px cover and 330px now-playing zone** (component says 60/320). `0010` corrects it.
  - The sidebar has an OUTPUT → **Devices destination with no page frame behind it**. Stage 8 therefore ships through the Output Device Popover (`vH78z`); `pv9Av` Device Row and `Y8Ojv` Status Pill belong to that unbuilt page.
  - The popover's "Up to 24-bit / 192 kHz" line needs **capability data the engine does not expose** — `hal.rs` is private with `pub(crate)` helpers, and `validate_output_format` only validates one requested format. `0011` calls for a narrow public capability query, not opening `hal` up.
  - **Token drift:** the Storage panels use `#151514` and `#111110`, which are in no token (nearest: `bg-surface #161615`, `bg-page #0F0F0F`), and the MANAGE nav group is raw hex. Needs a re-tokenize pass in Pencil before stage 11 rather than new near-duplicate constants in `theme.rs`.
- **Stage 9 deliberately left unplanned.** The Add Storage flow, scan progress/loading, and offline-root/scan-failure states are undesigned; a note now would invent surfaces. Blocker recorded on the roadmap row.
- Next: `0010` app shell, then `0011` device management. Blockers: Jason's hardware smoke pass (carried from stage 6 and 7), and Pencil design passes before stage 9.

## 2026-07-27 — Stage 7.5: app shell built (impl 0010)

- Landed `shell.rs` (sidebar, three nav groups, top bar, routed placeholder body, docked row) and `menu.rs` (macOS app menu + standard shortcuts). Nav routing is a plain `Destination` enum on the shell view, no router. Six new lucide SVGs plus a `pulse-mark.svg` traced from the design's brand path. No engine changes, no new dependencies.
- **The window went 1280×800 → 1440×900** to match the design frames. Later stages' content (album grid, Storage panels) is laid out for the 1204px right column left after the 236 sidebar, so matching the canvas keeps those at designed proportions. Plenty of the chrome is still fixed to its designed size — search 420, top bar 74, player 92, player sections 330/300 — but the right column and body flex, which is what the decision rests on: nothing assumes a 1440 window.
- **Row corrected to its in-context size** — cover 52×52, now-playing 330. Confirmed from the `E3N1P` instance overrides (`iIdh0`, `MVw5k`); the standalone `qKkw7` component really is 60/320, so the instance is what to trust.
- The window-wide drop target and the scrub mouse handlers moved from `PlaybackRow`'s root up to the shell root, which delegates into the row entity. The row's render is now just the 92-tall bar.
- **gpui-ce has no letter-spacing API at all.** The three group headers specify `letterSpacing: 0.8` and it cannot be built — grepped `gpui/src` and `gpui_elements/src` for `letter_spacing`/`tracking`, zero hits. First case in this project of the design genuinely fighting the framework rather than just being fiddly.
- **Rajdhani 600 is not embedded** — only Medium (500) and Bold (700) are vendored, and the design uses 600 for nav and Settings labels. Code says `FontWeight::SEMIBOLD` and GPUI matches to the nearest face. Vendoring the SemiBold TTF is the fix if it reads too heavy.
- gpui-ce menu findings, all verified against the pinned checkout rather than Zed's docs:
  - **`SystemMenuType` has only a `Services` variant.** `0010` says to use `MenuItem::os_submenu` for "Services, Window"; there is no `::Window` at this rev, so Window is a plain `Menu` with custom Minimize/Close actions and does not get AppKit's auto-managed window list.
  - **Element-level `.on_action` will not fire when nothing is focused** — `focus_node_id_in_rendered_frame(None)` falls back to the dispatch-tree root, whose path excludes child nodes. Window-scoped actions therefore use global `cx.on_action` plus `cx.active_window()`, not a handler on the shell's root div.
  - **An action with no registered handler is greyed automatically** — `validate_menu_item` returns `is_action_available`. About Pulse exploits this deliberately: the item sits in its standard position and is disabled until an About surface is designed.
  - Cmd+W quits, because closing the only window would otherwise leave Pulse running with no way to get one back.
- Corrections to `0010` found while building: the brand is a **one**-line copy block (`WA1Ph` has a single text child), not two; and the body inset is gap 18 on `E3N1P`, not the gap 20 the note took from the Storage screen.
- **Token drift in this stage's scope is a non-issue.** Every raw hex in the MANAGE group and the Storage badge (`#6A6A66`, `#9D9D99`, `#1E1E1C`, `#312F2C`, plus literal `"Geist Mono"`/`"Rajdhani"`/`6`/`4`) is an exact match for an existing token, so everything bound cleanly with no new constants. The genuinely-untokenized `#151514`/`#111110` are Storage-screen panels and still need the Pencil re-tokenize pass before stage 11.
- `make verify` green at 34 tests (22 engine, 10 app, 2 CLI). Codex crew review pass on the working-tree diff found no must-fix code issues.
- **Gotcha for every future UI stage: agents cannot verify the GPUI window in this sandbox.** `screencapture` fails with "could not create image from display" (no Screen Recording permission) and `osascript`/System Events fails with `-1719` (no Accessibility permission); only process enumeration works, which is enough to confirm the app launched as a regular GUI process and nothing more. The visual diff against the design, nav click-through, menu shortcuts, and the drop-to-play regression were therefore left to Jason rather than claimed. A headless option exists but is not free — `gpui::TestAppContext` needs `gpui` added as a dev-dependency with the `test-support` feature.
- Also worth knowing: the Pencil MCP needs `/Applications/Pen.app` actually running, not just its MCP server process. "transport not connected to app: desktop" means launch it — `open -a /Applications/Pen.app design/pulse-desktop.pen`.
- Next: `0011` device management. Blockers: Jason's interaction pass on this shell, his hardware smoke on the Matrix Mini-i Pro 4 (carried from stages 6 and 7), and the Pencil re-tokenize plus stage 9 design passes.

## 2026-07-27 — No text had ever rendered: `gpui_platform` was missing the `font-kit` feature

Jason ran the shell and screenshotted it: every icon, fill, border and radius correct, and **100% of text missing** — brand wordmark, nav labels, group headers, badge count, search placeholder, body heading, drop hint, and the whole playback row.

Severity **P1** — a common, user-visible failure that blocked every text-bearing UI workflow and every stage built on them. Not P0: no data loss, no crash, and engine validation and the CLI harness were unaffected. Against pinned gpui-ce rev `6c799b8e99`.

**Root cause: `gpui_platform`'s `font-kit` feature defaults to off, and `MacPlatform::new` silently falls back to `gpui::NoopTextSystem` when it is absent** (`crates/gpui_macos/src/platform.rs:193-196`). The entire real macOS text system, `gpui_macos/src/text_system.rs`, is `#[cfg(feature = "font-kit")]`. Pulse depended on `gpui_platform` with default features, so it had been running a no-op text system ever since the gpui-ce swap. The fix is one line in the workspace manifest: `features = ["font-kit"]` on `gpui_platform`. No new package or version and no rev-pin change — this activates `gpui_macos`'s existing *optional* `zed-font-kit` dependency, whose package was already locked because `gpui` enables it for its own crate. That is exactly why nothing looked missing: the lockfile diff is a single new dependency edge, not a new crate.

Verification status: feature resolution (`cargo tree -p pulse-app -e features -i gpui_macos`), system font count `11` → `370`, and `Rajdhani`/`Inter`/`Geist Mono` going from all collapsing onto one shared fallback `FontId` to three distinct real ids were all measured directly. **Jason then confirmed eyes-on that text renders in the live window.** The remaining `0010` acceptance checks — nav click-through, menu shortcuts, and the drop-to-play regression — are tracked separately below and are not covered by this fix.

- **It predated the shell.** Built `main` (8e21507, none of the shell work) in a throwaway worktree and reproduced the identical failure, so stage 7.5 was cleared as the cause before the fix was applied.
- **This corrects the 2026-07-26 stage 7 entry twice over.** Its claim that "the live window renders text fine" was never verified and was false. And its "GPUI's offscreen `render_to_image` omits all text, so automated pixel comparison of text is not possible" was not a tooling limitation at all — it was this same no-op text system seen from the offscreen side. Offscreen text comparison may be viable now; retry it before treating it as blocked.
- **Dead ends, recorded so nobody re-walks them:** the TTFs are well-formed with correct `name`-table families and PostScript names present, so the font files were never the problem; font-kit's `MemSource::add_font` silently dropping fonts whose `postscript_name()` is `None` (`src/sources/mem.rs:191`) looked promising but was a red herring; and switching `assets::fonts()` from `Cow::Borrowed` to `Cow::Owned`, to take font-kit's `Handle::from_memory` path instead of `Handle::from_native(CGFont)`, changed nothing. All of that was chasing a font-registration bug while the text system itself was a stub.
- **Why it hid for two stages, and the lesson.** gpui swallows every text failure with `.log_err()` (`crates/gpui/src/elements/text.rs`, on both `shape_text` and `line.paint`) and Pulse installs no logger, so a total text failure produced zero output and exit code 0. `NoopTextSystem` also *succeeds* at shaping and returns plausible advances, so layout reserved correctly-sized gaps and only glyphs were missing — which is why it read as a rendering bug rather than a missing text backend. `make verify` cannot catch any of it. **When debugging gpui text, check the enabled features on `gpui_macos` first — `cargo tree -p pulse-app -e features -i gpui_macos` — before suspecting fonts or the renderer.** To surface swallowed errors, temporarily add `log = "0.4"` (already in the lockfile) with a ten-line stderr `log::Log` impl.
- Still unguarded: nothing in the build fails if that feature is dropped again. The manifest carries an explanatory comment at the point of failure, but a real guard would need either a startup assertion or `gpui`'s `test-support` dev-dependency for a headless text test. Worth deciding before stage 8.

## 2026-07-27 — Session close: branch state and pickup point

Mission archived here. Stage 7.5 and the P1 font fix are both **complete and reviewed but deliberately uncommitted** — the mission authorized working-tree changes only. Anyone resuming starts from this snapshot.

Tree: branch `feature/stage-7.5-app-shell`, **0 commits ahead**, `main` untouched at `8e21507`. Fifteen files of working-tree change — two new source files (`shell.rs`, `menu.rs`), three modified (`main.rs`, `playback_row.rs`, `assets.rs`), seven new SVGs under `crates/pulse-app/assets/icons/`, `Cargo.toml` + `Cargo.lock` for the font fix, and this log. No diagnostic residue; the temporary `log` dependency, stderr logger, probe blocks, and throwaway worktree used to diagnose the font bug were all reverted and removed.

Verified: `make verify` exit 0 at 34 tests (22 engine, 10 app, 2 CLI), clippy under `-D warnings`, fmt clean. Codex crew reviewer reached a clean review over three rounds covering the full tracked and untracked diff, and confirmed the delegation of the drop/scrub handlers into the row entity by reading it. Text renders in the live window, confirmed eyes-on by Jason after the font fix.

**Not verified, and must be done before stage 7.5 is called accepted** — all four need a human, since agents cannot drive or capture the window here:

- Nav click-through: each item swaps both selection and body, with active styling following.
- `Cmd+Q` / `Cmd+W` / `Cmd+M` / `Cmd+H`.
- **Drop-to-play regression, the must-fix one:** drop a real audio file and confirm it plays, the row updates, play/pause toggles, and the progress bar seeks. This is the highest-risk check in the stage, because the shell moved the window-wide drop target and the scrub mouse handlers off `PlaybackRow`'s root and onto the shell root, which now delegates into the row entity.
- Hardware smoke on the Matrix Mini-i Pro 4, carried unresolved since stage 6.

Decisions left open, none blocking: whether to add a regression guard for the `font-kit` feature (an offscreen/headless glyph test is the candidate and may now be viable, since the stage 7 claim that offscreen rendering omits all text turned out to be the no-op text system rather than a tooling limit); whether to vendor Rajdhani SemiBold so nav and Settings labels hit the designed weight 600 instead of matching to the nearest embedded face; whether a greyed-out "About Pulse" is preferable to no About item; and whether `Cmd+W` behaving as `Cmd+Q` is acceptable while Pulse is single-window.

Housekeeping: `git worktree list` reports `/private/tmp/pulse-review-pr3-dc634a2` as `prunable`, left over from an earlier PR-3 review and unrelated to this work. `git worktree prune` clears the stale registration.

Next: land this branch, close out the four checks above, then `0011` device management. Still-standing blockers beyond those: the Pencil re-tokenize pass for the Storage panels' `#151514`/`#111110` before stage 11, and the stage 9 design passes for the Add Storage flow.

## 2026-07-27 — Stage 7.5 merged (session close)

- Branch committed (`73eedbf`) and merged to `main` as `e6fd6a4`. `make verify` green on the merge result at 34 tests; CI green on a clean machine, including the `Cargo.lock` check that guards the `font-kit` manifest change.
- Jason ran the shell and confirmed it looks right — the first look with text actually rendering. The remaining interaction checks (nav click-through, Cmd shortcuts, and the drop-to-play regression) were not walked through and stay open.
- Correction recorded against the stage 7 entry above: the claim that GPUI's offscreen renderer omits text "but the live window renders text normally" was wrong. The live window never rendered text either; the offscreen result had been showing the truth and was explained away as a tooling limit.
- Next session: `0011` device management. Carried blockers unchanged — Jason's hardware smoke on the Matrix Mini-i Pro 4 (open since stage 6), the Pencil re-tokenize pass before stage 11, and the stage 9 design passes for the Add Storage flow, scan progress, and failure states.

## 2026-07-28 — Stage 8: output device management built

- Added the `vH78z` playback-row popover with live Core Audio enumeration, confirmed active-device switching, UID-only app persistence, launch fallback messaging, relisting on open, permanent network empty state, and plain undesigned error text. The Devices nav destination remains the existing placeholder.
- Added the narrow engine `output_device_capabilities` query over advertised signed-integer PCM physical formats and a pure max-format test. Successful device changes now emit `OutputDeviceChanged`; a failed active switch restores the prior controller target instead of leaving UI and engine state split.
- The popover says "Exclusive during playback" rather than asserting idle per-device hog state. Pencil's 0.8 section-label tracking remains impossible at the pinned gpui-ce revision.
- Initial `make verify` was green at 39 tests. `make run` launched without panic until manually stopped. `pulse-cli devices` exited successfully but returned no devices in the sandbox, so live list equality, visual interaction, persistence across relaunch, audio switching, unplug/hog errors, and the Matrix hardware pass remain Jason-only checks.
- Reviewer round one fixed a capture-phase toggle race that reopened the popover, made transient launch-enumeration recovery resolve the saved UID again, restored the playback row's shipped 132px format block, clarified unsupported physical-format wording, removed an unreachable selection branch, and added app device-state tests. `make verify` is green at 41 tests after the fixes.
- Reviewer round two reported no remaining must-fix issues. Changes remain uncommitted on `feature/stage-8-device-management`; the GUI, persistence, and hardware checks above are the remaining acceptance work.

## 2026-07-28 — Stage 8 merged (session close)

- Merged via PR #8 as `a629a09`. `make verify` green at 41 tests (23 engine, 16 app, 2 CLI); CI green on both the branch commit and the merge.
- Code review of what landed: the engine boundary held as specified — `hal` is still a private module and the new helper is `pub(crate)`, with `device::output_device_capabilities` handing the UI a plain `OutputDeviceCapabilities { max_bits_per_channel, max_sample_rate }` plus a new `NoOutputCapabilities` error variant. App persistence is a single plain-text UID at `<config>/pulse/app-output-device.uid`, no serde or toml, separate from the CLI's config, with a parse test. `dirs` is the only new app dependency and was already a workspace dep.
- **Caveat found while reading, not a bug but decide before it ships to users:** `maximum_physical_format_capabilities` takes the bit-depth and sample-rate maxima *independently* across formats — its own test asserts that a device advertising 32-bit up to 96 kHz and 24-bit up to 192 kHz reports `(32, 192000)`. The popover can therefore display a pair the device does not support together. The design's "Up to 24-bit / 192 kHz" phrasing reads as two independent ceilings so this matches the mockup, and the query correctly filters to signed-integer PCM (excluding float), but on an audiophile-facing surface it is the kind of claim worth being deliberate about. Options if it matters: report the best supported *pair*, or reword to make the independence explicit.
- Still Jason-only, unchanged by the merge: visual check of the popover against `vH78z`, device switching by ear, persistence across relaunch, unplug/hog error paths, and the Matrix Mini-i Pro 4 hardware smoke open since stage 6. `pulse-cli devices` returns no devices inside the agent sandbox, so even the list could not be cross-checked there.
- Next: stage 9 is still blocked on design (Add Storage flow, scan progress, offline-root and scan-failure states). The Pencil re-tokenize pass for the Storage panels' `#151514`/`#111110` is also still outstanding before stage 11.

## 2026-07-28 — Device list verified

- Jason confirmed the popover's device list is correct against his real hardware. This was the one check agents could not do at all — `pulse-cli devices` returns nothing inside the sandbox — so the live Core Audio enumeration path is now verified end to end.
- Still open from stage 8: device switching by ear, persistence across relaunch, and the unplug/hog error paths. Still open since stage 6: the Matrix Mini-i Pro 4 hardware smoke.

## 2026-07-28 — Stage 9-10 backend planned (impl 0012)

- Wrote `0012-library-scan-and-store.md` covering the headless half of stages 9 and 10: storage roots, the file walk, tag extraction, and the SQLite store. The Storage screen stays out, still blocked on its Pencil passes.
- **Placement decided: library code lives in `crates/pulse-app` as a `library/` module, not a new crate.** I had proposed a `pulse-library` crate; Jason pushed back and was right. `arch/pulse-engine.md` only requires it be outside the *playback engine*, `pulse-app` is the only consumer, `pulse-cli` exists to prove engine and playback behavior rather than library behavior, and `pulse-app` modules are unit-testable as stage 8's preferences parser already shows. Extract to a crate if and when a second consumer appears.
- The realisation that unblocked this: only the Storage *screen* needs design. Scanning, tagging, and storing are headless, so they can be built and proven now — which also sidesteps the sandbox's inability to verify a GPUI window.
- Decisions recorded in the note: `rusqlite` with the `bundled` feature so a packaged `.app` does not depend on the host's SQLite; hand-rolled `std::fs` walk over adding `walkdir` unless symlink loops prove fiddly; FTS5 deliberately deferred since it is an additive migration; scan history stored because the Storage screen shows it and it cannot be backfilled; incremental rescan keyed on modified time.
- Testing follows the existing pattern — no binary fixtures anywhere in the repo, `pulse-engine` synthesizes buffers in memory. Store tests use `:memory:` SQLite, walk tests use temp dirs of empty files, and tag extraction generates a minimal WAV in-test.
- Next: run 0012 as a mission. Blockers unchanged — Jason's Pencil passes gate the Storage screen, the Matrix hardware smoke is still open from stage 6, and the Storage panels' `#151514`/`#111110` still need re-tokenizing before stage 11.

## 2026-07-29 — Stages 9-10 library scan/store backend built (impl 0012)

- Added the headless `pulse-app::library` module: SQLite-backed storage roots, tracks and scan history; a synchronous progress callback; hand-rolled recursion that skips symlinked files and directories; PCM-only Lofty extraction for FLAC, ALAC-in-M4A, AIFF and WAV; stable per-track cover-cache files; incremental path-plus-mtime rescans; conservative removal after traversal errors; and offline-root retention. FTS5 is explicitly deferred in schema v1.
- Path identity is a canonical UTF-8 path normalized to NFC, with Unicode lowercasing added only when macOS `_PC_CASE_SENSITIVE` reports a case-insensitive volume. An undefined `pathconf` result defaults to case-insensitive for NAS safety, while the original canonical spelling remains the displayed path.
- Reviewer round one separated valid-but-unsupported codecs from actual scan errors, added process-session recovery for interrupted history rows, accepted the engine-supported `twos`/`TWOS`/`none` big-endian PCM AIFF codes, made schema creation atomic, surfaced removal suppression, kept file paths on timestamp errors, stored unknown durations as null, and replaced the scan-long transaction with one short commit per changed file. Metadata I/O is outside transactions, progress callbacks run after commits, cache deletions follow database commits, and a second-connection test proves committed rows remain readable during scan progress.
- Reviewer round two caught artwork-cache failures rolling back otherwise valid track rows. The cache-error path now commits the metadata row with null or previous cover fields and records a non-fatal error; a generated WAV with an embedded ID3v2 cover proves an unusable cache path does not empty the catalog.
- Measured the final short-commit implementation on `/Volumes/Media/Music`: 4,904 candidates, 4,904 added, zero unsupported files, zero suppressed removals, and zero errors in 377.68s cold; the unchanged rescan skipped all 4,904 with zero errors in 47.79s, a 7.90× speedup. Lofty and the Symphonia playback reader produced no noticeable duration divergence across all 4,904 tracks. The expensive duration audit now requires a separate `PULSE_REAL_LIBRARY_COMPARE_DURATIONS` opt-in so a routine timing run does not perform a third pass over the NAS.
- `make verify` is green at 59 passing tests plus the intentionally ignored real-library timing diagnostic: workspace check, tests, clippy under `-D warnings`, and fmt. Dedicated generated-file tag coverage remains WAV-only; no FLAC or ALAC binary fixtures were added.
- Unsupported files are not persisted, so ambiguous-extension files such as lossy AAC `.m4a` are reopened and classified again on every rescan rather than benefiting from the mtime skip. The measured library has none; add a persisted unsupported marker only if this becomes material on a real mixed library.
- The concurrent Storage design pass was deliberately not opened or inspected. Current backend field names come only from impl 0012; `display_name`, `last_scan_at_ms`, and `is_reachable` are the likely cheap rename points when stage 11 binds the finalized design.
- Reviewer reached a clean verdict after three rounds with no remaining must-fix issues. Next: land the now-authorized PR. Storage UI remains stage 11 work and is still gated on the concurrent Pencil pass.

## 2026-07-30 — Storage/library design passes complete, stage 11 spec written (0013)

- Two design sessions in `design/pulse-desktop.pen`, run concurrently with and after the 0012 mission. First pass (2026-07-29): the four stage-9 design blockers — Add Storage modal (`cJCbr`), scanning state (`NK6lk`), offline-root and scan-failure states (`e2xOQ`) — plus the re-tokenize pass, which was wider than previously logged: besides `#151514`/`#111110`, the selected-row tint `#1A1117` and the lime pill pair `#111A0B`/`#4F7B22` were also untokenized. New tokens: `bg-surface-alt`, `bg-selected`, `quality-soft`, `quality-border`, `danger-soft`, `scrim`; `#151514` collapsed into `bg-surface` (1/255 apart). 27 nodes rebound, zero leftovers. Committed as `9ccd752`.
- Second pass (2026-07-30): audited design coverage against `mvp.md` and designed the five missing stage-11 cases — Album Detail (`XaIfA`), Albums first-run empty (`lTVhl`), Storage empty (`VPPvT`), Playlists empty (`LNdSq`), and the Search Results popover (`A3JuUc`, design only). All screens verify at 0 layout problems. **This pass is in Pen.app memory but not yet saved to disk — Jason must save in Pen.app before it can be committed.**
- Schema gap found while writing 0013: the design and `mvp.md` both require `year` (release-year sort, album header) and `genre` (filter chips) but 0012's tracks table has neither. Recorded as required additive backend work in 0013, alongside album/all-tracks/tracks-for-album queries and `rename_storage_root`. The Tracks screen's Liked chip has no MVP backing and is skipped at implementation.
- File-hygiene fix in the .pen: eight `Info / *` frames were named for different labels than they render (e.g. `Info / Last scan` rendering "Formats") — one edit landed on the wrong row because of it. All renamed to match their labels.
- Wrote `0013-library-ui.md` as the stage 11 spec and updated the roadmap: stages 7-10 moved to Completed, stage 11 is Current, search implementation proposed for stage 12.
- Pencil tooling notes: the MCP API changed to a JS `execute` surface (old `batch_design`/`get_variables` tools are gone). The known new-node render glitch (blank paint, phantom +50px offsets, false clip warnings) reappeared and was cleared by focusing Pen.app with `open -a`; layout verification is unreliable while it is active.

## 2026-07-30 — Stage 11 MVP library UI built (impl 0013)

- Added schema v2 with `year` and `genre`, album/all-tracks/album-tracks queries and MVP sorts, explicit Unknown Album/Unknown Artist grouping, root rename, scan cancellation, generated-WAV tag coverage, plain library view-models, and the Albums, Album Detail, Tracks, Playlists-empty, and full Storage surfaces wired into the app shell and existing single-file playback controller.
- Backfill decision: migrate v1 to v2 with a one-shot metadata re-read before the schema transaction. Successfully parsed rows receive year/genre immediately; unreadable rows keep null metadata and get `modified_at_ns = -1` so the next reachable scan retries them. No FLAC or ALAC binaries were added; dedicated year/genre extraction remains proven with the generated in-test WAV.
- The pinned gpui-ce revision exposes `App::prompt_for_paths(PathPromptOptions)` and its macOS backend already uses `NSOpenPanel`, so Add Storage uses that API and no AppKit bridge or dependency was added. Search stays inert, Liked is omitted, New Playlist is visibly disabled, and no queue behavior was introduced.
- Added an ignored agent-runnable proof that scans generated WAVs into a temporary on-disk SQLite database, reopens it, and prints album count, one album's ordered tracks, and Online/Offline derived root states. The real NAS was not accessed from the sandbox. `make verify` is green at 71 passing workspace tests plus 2 intentionally ignored diagnostics, including clippy under `-D warnings` and fmt.
- Reviewer round one fixed a cross-root scan race: while any root is scanning, Add/Edit/Remove/Rename are now unavailable, and confirm paths retain the modal or draft and show an error if the store is checked out rather than silently discarding user intent. The mid-scan album fallback now trims album and artist tags exactly like the SQL grouping, with view-model coverage.
- Reviewer round two reported no remaining must-fix issues after independently rerunning `make verify`. Accepted risks: the one-shot v1-to-v2 metadata backfill runs synchronously during startup and can block for a large or slow NAS library, while scan-worker panic recovery remains deferred with stage 13 failure hardening. Add Storage input focus and Remove-modal Escape handling remain part of the manual GUI pass.
- Post-PR runtime smoke caught a P0 startup panic that unit tests had missed: the album query ordered Date Added, Release Year, and Duration by aggregate aliases that were not declared in the `SELECT`. The query now aliases every aggregate result, and a regression test executes all five album sort orders. `make run` stays up against the same migrated on-disk library, and `make verify` is green at 72 passing workspace tests plus 2 ignored diagnostics.
- GUI rendering was not verified in the agent sandbox. Jason still needs to run the scratch-config first-run flow, add a root and watch scan progress, browse/open an album and play a track, unplug an offline root, force a scan failure, and recheck drop-to-play. Rajdhani SemiBold vendoring remains the pre-existing approximation.

## 2026-07-31 — Stage 11 merged (session close); next session is the manual acceptance pass

- Stage 11 merged via PR #10 as `18ff620` with CI green (Rust / macOS, 1m05s). The branch carried two commits: the library UI build (`3b132a8`) and the post-PR P0 fix for the album-sort aggregate aliases (`276adbf`). Local and origin `main` are in sync. `make verify` was green at 72 workspace tests plus 2 ignored diagnostics on the final branch state.
- With stages 9-11 all landed inside three days, almost none of the accumulated human verification has happened yet. **The next session is deliberately not stage 12 — it is the manual acceptance pass over everything merged.** The backlog, oldest first:
  - Stage 6/7 (carried): hardware smoke on the Matrix Mini-i Pro 4.
  - Stage 7.5: nav click-through with active styling, `Cmd+Q` / `Cmd+W` / `Cmd+M` / `Cmd+H`.
  - Stage 8: device switching by ear, UID persistence across relaunch, unplug/hog error paths.
  - Stage 11, the big one: scratch-config first-run flow (empty Albums → Add Storage → native folder picker → modal → watch live scan progress), browse Albums, open Album Detail, double-click a track to play, sort/filter chips on Albums and Tracks, root rename/remove, unplug the NAS root for the offline state, force a scan failure (chmod a subfolder) for the FAILED row and Retry, Add Storage input focus, Remove-modal Escape, and the drop-to-play regression that every shell change re-risks.
  - Visual pass of each new screen against its `.pen` counterpart (`E3N1P`, `XaIfA`, `KfJr9`, `MHrLm`/`LNdSq`, `C6IrDC` + state variants) — agents cannot do this in the sandbox.
  - One non-GUI caveat to feel out on real data: the one-shot v1→v2 backfill runs synchronously at startup and can block on a large or slow NAS library.
- Stage 12 (queue, playlists, search implementation) starts only after the pass; bugs found tomorrow get triaged into fix-now versus stage 13 hardening.

## 2026-07-31 — Manual acceptance bugs #11–#15 fixed in the working tree

- Output capability display now retains signed-integer bit/rate ceilings when available and falls back to a rate-only ceiling from mixable float linear-PCM formats, so AirPods-class devices render `Up to N kHz` without an unsupported-device message. This is display-only: hog mode, nominal-rate switching, physical-format selection, and playback behavior are unchanged pending Jason's AirPods hardware test.
- Capability-query notices are tracked separately from other device notices and replaced on every query instead of appended, fixing repeated-popover message growth without changing the stage-8 capture-phase toggle behavior. Pure view-model coverage proves the rate-only label and stable repeated refresh message.
- Album Detail uses content-based header sizing without a fixed-height text box; the other fixed-height text containers in `albums.rs` are single-line/truncated controls, fixed table rows, covers, or empty-state centering and do not have the same fallback-font stacking hazard. Albums Detail and Tracks now each keep transient row selection state: single-click selects with `bg-selected`, while double-click still plays.
- Jason's live acceptance pass then exposed the same hard-cap problem in the 63px page headers: Storage's subtitle painted under the summary cards. Albums, Tracks, Playlists, and Storage page headers now size to their content with a 63px minimum and explicit bottom spacing. The pass also exposed that the Albums Grid/List control was a hard-coded visual stub despite list browsing being in `mvp.md`; it now switches transient view state and renders a clickable album table using the existing Tracks-table language.
- A full-resolution comparison of Jason's Album Detail screenshot against Pencil `XaIfA` found that the implementation had lost the design's 18px Back/header/table rhythm and bottom-aligned content-sized album header. The GPUI tree now mirrors that structure, including the bordered cover, bold Rajdhani Back label, 8px copy stack, semibold artist, disabled bordered Shuffle/More silhouettes, HI-RES border, and pink left/bottom edge on the playing row. Album Detail now owns a tracked scroll handle reset on album open so a prior detail position cannot make a newly opened album start midway through its tracks. Shuffle and More remain deliberately inert until stage 12 queue/action semantics exist.
- Scanning now falls back from missing embedded artwork to case-insensitive `cover`, `folder`, then `front` files with `jpg`, `jpeg`, then `png` extensions. It ignores booklet names, writes through the existing stable `<track_id>.cover` cache path, and fills NULL covers on mtime-skipped tracks through a directory check without reopening audio metadata. Generated-WAV/temp-directory tests cover preference, booklet exclusion, incremental fill, and replacement without duplicate cache files; no binary fixtures were added.
- `make verify` is green at 79 passing tests plus 2 intentionally ignored diagnostics: workspace check, tests, clippy under `-D warnings`, and fmt. Changes remain uncommitted on `fix/device-popover-and-library-ui`; GUI and hardware behavior were not verified in the agent sandbox.
- Next: Jason's manual pass — AirPods show `Up to N kHz` with no amber message; repeated popover opens keep one message; all library page subtitles remain clear of following content and Albums switches between working Grid/List layouts; 菲靡靡之音 keeps the table below clickable buttons and Play Album works; single-click selects and double-click plays in Album Detail and Tracks; and 菲靡靡之音 / Body And Soul show folder art after rescanning `~/qobuz`.

## 2026-07-31 — Live cover rendering and selected-track playback follow-up

- Jason's next live pass showed that even albums with populated `cover_art_path` values rendered blank. The cache contained valid JPEGs; the UI converted each filesystem path to `String`, which GPUI interprets as an embedded asset name. Covers now pass a `PathBuf` to `img`, selecting GPUI's filesystem resource loader.
- Single-click selection previously changed only the row background, leaving the playback row empty and its Play control inert. Selecting a track now loads its path/title context into an idle playback row without starting audio; the central Play control starts it, while double-click remains immediate playback. Active playing/paused sources are not replaced by a later single-click selection.
- The live database contains no rescan after the original 86-track import, so 菲靡靡之音 and Body And Soul correctly still have NULL cover fields despite their `folder.jpg` files. Jason must run Rescan with this build to exercise the incremental folder-art fill.
- `make verify` is green at 81 passing tests plus 2 ignored diagnostics, including clippy under `-D warnings` and fmt. Next: reviewer follow-up, then Jason rechecks cached artwork, single-click/load + Play, double-click/play, and the two folder-art albums after Rescan.

## 2026-08-01 — Folder art live-verified; playback-row artwork follow-up

- Jason confirmed that Rescan populated the previously artless albums. The live second scan updated 21 tracks with zero errors, and the cached covers now render in Albums; this verifies the mtime-skipped incremental folder-art path end to end.
- The playback row still showed its Stage 7 placeholder because it had no artwork state. Library selection, double-click playback, and Play Album now carry the track's existing cached-cover path into `PlaybackRow`; its 52px artwork well uses GPUI's filesystem image source, while drag-and-drop explicitly clears any prior library cover.
- Reviewer round five found no must-fix issues in the artwork plumbing and independently reproduced `make verify` at 81 passing tests plus 2 ignored diagnostics.
- The native macOS title strip is now transparent and titleless so app content occupies the unified window chrome. The sidebar reserves a 44px top inset, leaving the traffic lights clearly separated from the Pulse brand. Jason accepted this layout for the current branch; broader responsive-layout work remains outside this acceptance-fix set.
- Final local `make verify` is green at 81 passing tests plus 2 ignored diagnostics, including clippy under `-D warnings` and fmt. Next: one final working-tree review over the titlebar delta, then land the now-authorized PR and merge after CI is green.

## 2026-08-01 — Acceptance-fix branch merged (PR #16); acceptance pass largely green

- Merged `fix/device-popover-and-library-ui` via PR #16 as `eb2f318` with CI green (Rust / macOS, 1m24s). Issues #11–#15 auto-closed. Local and origin `main` in sync; final state 81 passing tests plus 2 ignored diagnostics.
- The test library at `~/qobuz` grew during the pass to 8.6 GB / 206 FLAC tracks / 15 album folders across 8 artists, including two multi-album artists (王菲 ×7, 陶喆 ×3), a self-titled album (陶喆/陶喆), and CJK/parenthesized names throughout. The live database now holds 206 tracks with 183 covers. The 23 coverless tracks are 唱游 and 浮躁, whose folders contain no artwork in any form — embedded or file — so their placeholders are correct behavior, not a regression; Di-Dar's camera-roll scans (`P1010083.JPG`…) are ignored by design and its covers come from embedded art.
- Verified live during the pass, beyond the five issue fixes: cached covers render everywhere including the playback row's artwork well, single-click loads a track into the idle playback row with Play arming it, double-click plays immediately, Rescan's incremental folder-art fill (21 tracks updated, zero errors), the Grid/List album toggle, content-sized page headers on all four library pages, and the new transparent unified titlebar with the 44px sidebar inset.
- Still open from the acceptance backlog: AirPods playback by ear (decides whether #11 needs a shared-mode follow-up beyond the copy fix — hog and 44.1 kHz nominal on Bluetooth remain untested), the Matrix Mini-i Pro 4 hardware smoke (stage 6), stage 7.5 shortcuts (`Cmd+Q/W/M/H`), stage 8 by-ear switching / relaunch persistence / unplug-hog paths, and stage 11's failure-state checks: scratch-config first-run flow, root rename/remove, NAS-root unplug for the offline state, a forced scan failure with Retry, and the drop-to-play regression.
- Next: finish the remaining manual checks above, then stage 12 (queue, playlists, search) per the roadmap.

## 2026-08-01 — Stage 12 queue, playlists, and search built (impl 0014)

- Added schema v3 with cascading playlist tables, duplicate-friendly CRUD and append/remove/move operations, contiguous move renumbering, aggregate playlist listing, and capped LIKE search across albums, tracks, and playlists. Fresh, v1, and genuine v2 databases all migrate to v3; in-memory coverage includes duplicate names and entries, both cascades, move directions, aggregate cover fallback, ASCII case-insensitive matching, CJK substrings, album-artist hits, empty results, and per-group caps.
- Added a plain transient `QueueState` (`Vec<TrackRef>` plus index) without engine/controller changes. Every album, filtered/sorted Tracks view, and playlist play or armed selection rebuilds the queue in visible order; Ended advances, Next disables at the end, Previous restarts only after 3 seconds, and the inert queue button shows only the remaining-count badge. Shuffle and Repeat remain inert, and the playback secondary line is `artist - album` for library tracks.
- Replaced the Playlists placeholder with the populated list/detail design, one create/rename name dialog, count-bearing delete confirmation, selected-middle-row play behavior, and right-click menus on Album Detail, Tracks, and playlist detail for Play, Add to Playlist, New Playlist, and playlist-only removal.
- Reorder decision: shipped the sanctioned Move Up / Move Down context-menu fallback. The pinned gpui-ce revision has generic drag hooks but this codebase has no internal row-drag/drop-indicator pattern; building and validating that interaction would dominate this final feature stage, while the fallback preserves the required ordered-store behavior.
- Search now uses a 150 ms revision-guarded debounce, grouped Pencil popover, keyboard wrap/activation/dismissal with automatic selected-row reveal, click activation, and macOS text-input/IME handling for CJK. A loading state suppresses false no-match rows during the debounce. `Cmd+F` was cheap through the existing menu/action plumbing, so it ships in the Edit menu and focuses the field; track hits play with their album as queue context.
- Read-only Pencil checks covered `qKkw7`, `MHrLm`, `Lr7A3`, `cYA3i`, and `A3JuUc`; no design file was modified. The ignored Stage 12 proof scans generated WAVs into a temporary on-disk library, creates and reorders a playlist, and prints the queue produced by play from its middle row.
- `make verify` is green at 100 passing workspace tests plus 2 intentionally ignored diagnostics: workspace check, tests, clippy across all targets under `-D warnings`, and fmt. The Stage 12 ignored proof also passes with `--ignored --nocapture`.
- Both working-tree review rounds finished clean with no must-fix issues, and the reviewer independently reproduced all checks after the focused search UX delta. Known MVP limitation: the playlist-name dialog retains the Stage 11 `key_char` append input, so CJK IME composition and caret movement are not supported there; search has the full IME path because CJK search is explicitly required. Context-menu placement uses window-coordinate offsets without edge clamping and remains part of the GUI pass.
- GUI rendering was not verified in the agent sandbox. Next: Jason manually checks track-boundary auto-advance on 菲靡靡之音, Next/Previous with the 3-second rule, badge countdown, playlist create/rename/delete, Add to Playlist from all three surfaces, Move Up / Move Down reorder, play from a selected middle row, search for 王菲 / `frank` / gibberish with Enter and Escape, CJK search IME composition including backspace mid-composition, and context-menu alignment plus near-edge placement.

## 2026-08-01 — Stage 12 manual-pass follow-ups

- Jason's live pass has tightened the shipped MVP across Album Detail/table alignment, the Playlists empty-state action, responsive album-card sizing and spacing, the All Albums heading, and native macOS chrome. The titlebar now has correctly positioned traffic lights and safe drag regions, the sidebar branding spacing is improved, and the app uses the exact Pencil icon asset.
- Add Storage now starts with the designed modal before invoking the native folder picker. Tracks are backend-paginated with filter-aware totals and compact controls while playback still builds the queue from the full active sort/filter result across page boundaries; matching pagination was added to the Albums design for the next implementation step. Shuffle and Repeat remain visible but intentionally inert; the queue control remains the specified inert badge-only control, with no queue drawer added.
- Added `docs/impls/MANUAL_ACCEPTANCE_CHECKLIST.md` as the consolidated remaining human-only MVP checklist. `make verify` is green at 105 passing tests plus 2 intentionally ignored diagnostics, and the Stage 12 ignored proof passes; the complete delta is going through the final working-tree review before PR.
- Next: land this reviewed progress, then continue Jason's manual checklist. Albums backend pagination and any remaining focused Album Detail polish stay as explicit follow-up work rather than expanding this PR.

## 2026-08-01 — Stage 12 merged (PR #18); docs archived and stage 13 planned

- Stage 12 merged as `7733a00` with CI green (2m22s). Independently reverified on the merge result: `make verify` green at **105 passing tests plus 2 ignored** (79 app + 24 engine + 2 CLI), clippy under `-D warnings`, fmt clean. Local and origin `main` in sync. **All MVP features are now built** — stages 1-12 complete, nothing feature-shaped left before v0.
- Note for the record: the branch also carried a `design/pulse-desktop.pen` change (+439 lines, Albums pagination) and the exported app-icon PNG, added during Jason's live follow-up pass. The mission's original read-only design constraint was superseded by that live direction, not violated silently.
- **Retired `0005-product-design.md`.** It had sat in "In Progress" for the entire project because its own text said it could never be globally complete — a permanent catch-all rather than a plan. Every surface it named now exists in Pencil, plus album detail, empty/first-run states, storage state variants, search, the playlist dialog, and the context menu. Archived, with the genuinely-undesigned surfaces written down concretely instead: `mvp.md`'s Design Status now lists only the four stage-13 states, and names track inspector / queue drawer / Devices page / Settings screen as deliberately out of the MVP.
- Archived the six shipped impl notes (0009-0014) per the roadmap's own rule, rewrote every link, dropped the now-empty In Progress section and a duplicated stage-12 row, and added `docs/features/README.md`. `docs/impls/` now lists only live documents: the roadmap, this log, the acceptance checklist, and stage 13.
- Wrote `0015-mvp-states-and-hardening.md`. Key finding while scoping it: **stage 13 is much narrower than the roadmap row implied** — empty, offline-storage, scan-failure, scan-progress, and device-unavailable states all shipped in stage 11/#16. What actually remains is four items: loading states (the v1→v2 startup backfill can visibly block on a large NAS library), decode failure (skip-and-report rather than halting the queue), hogged/unavailable device *during* playback with plain-language copy and a recovery path, and missing-file handling that must not delete rows — that would collide with the offline-root guarantee. Each needs a Pencil pass first, per the standing rule.
- Carried forward: volume control is specced (`docs/features/01-volume-control.md`, issue #17) but unstarted, gated on a playback-bar design pass. The AirPods playback-by-ear check is still open and still decides whether #11 needs a shared-mode follow-up.
- Next: walk `MANUAL_ACCEPTANCE_CHECKLIST.md` (96 checks). Findings triage into fix-now versus stage 13, then stage 13, then volume control, then stage 14 packaging and v0.

## 2026-08-02 — macOS packaging infrastructure built

- Added the hand-rolled arm64 `Pulse.app` bundle path, versioned and signed DMG, credential-gated Developer ID signing and notarization targets, tag-triggered draft release workflow with an exact tag/version guard, and local release runbook. The immutable bundle identifier is `com.wycstudios.pulse`; the binary and plist both target macOS 12.0.
- Kept the app non-sandboxed with no entitlements file, App ID, or provisioning profile. Nothing demonstrated a need beyond hardened-runtime defaults: Pulse uses audio output, not input, and non-sandboxed file access needs no entitlement.
- Verified repeat `make bundle` runs, plist validity and values, all ten standard icon representations, the executable path and arm64 Mach-O target, an isolated ad-hoc hardened-runtime signature with the expected identifier, and a valid unsigned structural `hdiutil` DMG. The resulting app is 18 MB and the compressed DMG is 7.8 MB.
- `make verify` remains green at 105 passing tests plus 2 intentionally ignored diagnostics. The workflow YAML parses, its keychain import and identity discovery block is byte-for-byte identical to Runner's, and every signing/notarization credential guard fails with a variable-specific error when tested independently.
- Real Developer ID signing and Apple notarization were not invoked. GUI launch, TCC, storage picker, playback, hog mode, application-data resolution, and quarantined Gatekeeper behavior remain Jason's manual release checks.

## 2026-08-02 — Packaging merged (PR #20) and independently reviewed

- Merged as `ffd8075`. Independently reverified rather than taking the report on trust: `make bundle` succeeds from a clean tree and produces `target/release/Pulse.app` at **18 MB** with the correct structure (`Info.plist`, `MacOS/pulse-app`, `Resources/Pulse.icns` at 194 KB), `plutil -lint` passes, and `otool -l` confirms the binary's `LC_BUILD_VERSION` is `minos 12.0` against SDK 26.2. Build artifacts are correctly gitignored. The workflow's keychain-import and identity-discovery block is the runner/quill pattern unchanged, and `TAURI_SIGNING_*` was correctly left out.
- Nice touch not in the spec: the release workflow guards that the git tag matches the workspace version before building, and creates the GitHub release as a **draft** rather than publishing directly.
- **Three review findings, none blocking, none fixed yet:**
  - *`LSMinimumSystemVersion 12.0` is an unverified claim.* The plist and `MACOSX_DEPLOYMENT_TARGET` both assert macOS 12, but nothing has confirmed gpui-ce actually runs there — if it needs newer Metal APIs, the app would install on macOS 12 and then crash. There is no macOS 12 machine here to test on. Either verify on an old OS, or raise the floor to a version we are confident about. Do not leave it as a silent guess before v0 goes out.
  - *Notarization staples the DMG but not the `.app` inside it.* Standard and acceptable — Gatekeeper checks the DMG on download — but a user who drags the app out and first launches it offline makes Gatekeeper fall back to an online check. Stapling the `.app` before building the DMG would remove that edge.
  - *`codesign --deep` is used for verification* in `make sign` and the runbook. Apple deprecates `--deep`, and Pulse's bundle has no nested code for it to reach anyway, so it can simply be dropped.
  - Minor: `Makefile` line 1 evaluates `VERSION := $(shell ./script/bundle-mac --print-version)` at parse time with a hard `$(error)`, so *every* target — `test`, `verify`, `build` — now fails if that script is missing or not executable. Confirmed by invoking `make -n test` from another directory. Harmless from the repo root, but unnecessary coupling between the packaging path and the everyday build.
- **Nothing about the signed path has been executed.** No real `codesign` with the Developer ID identity, no `notarytool` submission, no CI release run — the first `v*` tag is the first real exercise of that workflow. `make sign` / `make notarize` credential guards were confirmed to fail with variable-specific errors, which is all that could be checked here.
- Next and load-bearing, all Jason's: launch the **bundled** `Pulse.app` rather than `cargo run` and confirm `/Volumes/Media` still reads under the network-volume TCC gate, the Add Storage picker opens, playback and hog mode work on the Matrix DAC, and library data resolves under `~/Library/Application Support/pulse`. That TCC difference is the whole reason packaging was pulled ahead of stage 13. Then the acceptance checklist, then stage 13.

## 2026-08-07 — Stage 13 built: failure states, live UX pass, structured error kinds (impl 0015)

- Built the four 0015 gaps on `feature/stage-13-mvp-states` (working tree, unmerged): queue-level failure behavior (missing-file existence check at play time with runtime row marks that never delete rows, decode-failure skip-and-report, poison-queue stop with a plain message), hogged/device-loss recovery (pid resolved to an app name via `NSRunningApplication`, Try again resumes at the failure position via a confirmed `PlayAttempt`, Choose output opens the popover, plus a ~2 s watchdog because the engine emits **no event at all** when the device disappears mid-playback), and loading states (store open + v1→v2 backfill moved off the main thread with determinate progress, open-failure Retry state replacing the old `.expect` panic, list-level loading placeholders so an untouched library never reads as empty). All surfaced through a dismissible notice banner above the playback row; presentation is plain-language pending the concurrent Pencil pass.
- **Option A landed: structured playback error kinds.** `PlaybackEvent::Error` now carries `PlaybackErrorKind { Track, Device { hog_pid } }` mapped from the source `EngineError` at the controller's emit sites, and the app's string-prefix classifier is deleted. New `EngineError::AudioUnit` variant stops auhal.rs disguising device/start failures as `UnsupportedFormat` — review had caught that the string classifier would have burned through a whole queue on a device problem.
- **Decisions recorded** (rationale and measurements in [`../arch/thread-model.md`](../arch/thread-model.md)): page queries stay synchronous on the UI thread (0.06–0.16 ms live, ~25–40 ms at a synthetic 50k tracks; audio unaffected by architecture; the traded-away query-time loading state is dead by construction); one process, no split playback engine (the controller channels are the future IPC seam); related-data deletion is application-owned — `remove_storage_root`/`delete_playlist`/`delete_track` delete children explicitly in order, the schema's CASCADE clauses are dormant safety nets until the repo-layer refactor's v4 rebuild converts them to plain `REFERENCES`.
- Jason's live direction reshaped surfaces mid-mission: albums are grid-only (list view deleted) with infinite scroll over a new SQL `album_page` (HAVING-clause chip filters, total count), tracks/list page size 20 with always-visible pagination footers, a `components.rs` module (buttons, quality badge, caret, `render_text_input`) so styling lands in one place, text-input carets and I-beam cursors, playlist-modal spacing matched to Add Storage, the fake sidebar collapse chevron removed, idle playback copy now points at the library, and `bg_selected` brightened (design `.pen` swatch needs a matching update by the design session).
- Live testing flushed out an id-recycling bug class: removing a root recycles SQLite track ids, so `{id}.cover` cache paths served **stale pixels** from gpui's path-keyed image cache (disk bytes were verified correct) — cover paths are now content-unique `{id}-{fnv1a}.cover` with superseded files deleted, which also fixes changed-artwork-on-rescan never displaying until restart; runtime missing marks had the same staleness and now clear on root removal and after presence-verifying scans (see the findings bullet below for the exact gate). Also diagnosed, not a Pulse bug: several 王菲 reissues carry a literal `ALBUMARTIST=######` from Qobuz — fix is a NAS retag.
- Dock icon: the Pencil export is full-bleed while macOS expects the Apple grid margin (runner's icon: ~80% content) — generated `app-icon/dock.png` (824/1024 centered) now feeds both the dev `setApplicationIconImage` and the bundle's icns; the full-bleed art stays for the in-app brand mark. `make bundle` re-verified.
- The reviewer's full final pass returned eight findings, all fixed the same day: terminal playback events now carry a per-`PlayFile` attempt ordinal (the controller counts commands, the row counts dispatches — FIFO makes them agree) so stale `Ended`/`Error` events from a superseded play can no longer skip or mark a freshly installed queue; missing-mark clearing is gated on scans that actually verified presence (completed outcome, removals not suppressed — not cancelled/offline/failed); every worker catches unwinds so a panic surfaces as a recoverable state instead of stranding boot/scan/event channels (forced-panic engine test included); `album_page` sorts end in the exact GROUP BY pair for a total order under LIMIT/OFFSET (tied-albums paging test); a failing album page query latches a stall flag instead of retrying on every render; the explicit-deletion tests run with `PRAGMA foreign_keys = OFF` and seeded children so the dormant cascades cannot mask a missing application-owned delete; and the thread-model/packaging/0015 docs were corrected (the single-process rationale now admits what a split *would* buy — in-process native code exists — and why it still loses on IPC/TCC cost).
- More Jason-directed UX in the same window: genre tags are split on commas at query time (Qobuz writes "Musiques du monde, J-pop, Japon" as one string), so chips are per-genre and filtering matches list membership in SQL for both tracks and albums; the redundant albums section subtitle was removed after checking the design never had one; sort controls use an `arrow-up-down` icon (swapped in the design across all six screens, then in code); `Album.genres` turned out to be dead plumbing after the SQL filter move and was removed along with its per-page aggregation query; and the artist filter became a real picker — a popover with a search input (standard `render_text_input`, type-to-filter, Enter picks the first match, Esc closes), per-artist track counts from a new `store.artists()`, and a Clear row, designed first in the `.pen` (new "Library / Tracks — Artist Filter" screen) and then implemented. Note: the Pencil editor's live layout wedged during those edits — the document data verifies correct via reads, but the popover needs an editor reload to render; flagged to Jason.
- The re-review returned three findings, fixed same day: the artist picker got the output picker's press-closed guard (clicking the open trigger no longer closes-then-reopens) and closing now notifies unconditionally via `choose_artist_filter` (previously, picking the already-active artist or clearing an empty filter hit `set_artist_filter`'s unchanged-value early return and left the popover painted); genre membership moved from wildcard `LIKE` to a `genre_has_member` scalar SQL function registered on the connection, sharing `genre_tag_members` with the chip enumeration so both sides split/trim identically — a `Jazz,  Modal` tag's Modal chip now matches, and `%`/`_` in a genre are literal, with exact-membership tests across chips, track filter, and album HAVING (including the secondary-member album path); two doc lines still describing clear-on-every-scan-finish were corrected to the gated behavior.
- `make verify` green at 130 tests (103 app + 25 engine + 2 CLI, 2 ignored). Next: reviewer's re-review of the settled tree, Jason's manual checklist (corrupt file mid-queue, rename-after-scan, hog mid-playback, DAC unplug, NAS cold launch, album infinite scroll, artist picker, cover correctness after remove/re-add), then the runner-style repo-layer refactor as its own slice, then stage 14.

## 2026-08-07 — Queue popover built (feature 03)

- Implemented `docs/features/03-queue-popover.md` on `feature/queue-drawer` (working tree, uncommitted, on top of the design/spec commits). Queue mutations live in `queue.rs` beside the advance logic: `jump_to` moves the index and drops nothing (Previous still walks back over jumped entries), `remove_at` renumbers and never removes the current track (removing an earlier entry shifts the index so the current track stays current), `clear_upcoming` truncates at the upcoming boundary so history stays for Previous. `TrackRef` gained `duration_ms` (u64 from the store's i64) for row durations and the header minutes; `upcoming()` yields 1-based absolute queue positions, `upcoming_duration_ms()` excludes unknown durations.
- The popover matches Pencil node `qfVOF` exactly (376w, right edge on the trigger, bottom 71px above the trigger bottom, max-h 541, padding 14 / gap 11): header with mono `N UP NEXT · M MIN` meta and a Clear button (both hidden at zero upcoming), NOW PLAYING block in `bg_inset` with accent border, `audio-lines` icon, and live `elapsed / duration` in quality lime, numbered UP NEXT rows (click jumps; hover fills `bg_muted` and reveals the ✕ remove, which `stop_propagation`s so it never jumps), muted "Nothing up next" empty line. Popover times drop the leading minute zero per the design (`format_queue_time`). Dismissal is the stage-8 pattern plus the artist picker's press-closed guard; Escape works via `track_focus` on a row-owned `FocusHandle` focused on open (an `Option` because tests build the row without a window). The trigger icon renders accent while open; the badge binding is untouched.
- The jump path reuses the queue dispatch machinery wholesale: `play_queue_track` was split so its cx-free half (`prepare_queue_play`) is unit-testable, meaning jumps inherit the stage-13 attempt ordinals (stale terminal events from the superseded track cannot move the jumped queue) and the missing-file skip-and-report path unchanged.
- Review round one found two real holes, both fixed: (1) a jump or Next into an all-missing tail exhausted the queue and posted "Playback stopped" while the superseded track kept playing — `play_queue_track`'s exhaustion branch now sends `Stop` when playback is Loading/Playing/Paused (`active_playback_needs_stop`), and `now_playing_lines` requires `queue.current()` to match `source_path` so the block describes the audible track, not the never-played entry the index landed on; natural Ended/Error exhaustion sends nothing. (2) the up-next list built every row on every 100 ms position tick — it is now a virtualized `uniform_list` (`Infer` sizing, uniform 52px stride = 50px row + 2px gap, visible range fetched through `cx.processor`), so a full-library queue builds ~10 rows per frame instead of all of them. `render_upcoming_row` returns `AnyElement` because the processor's closure lifetimes reject the opaque `impl IntoElement`.
- Re-review clean, no remaining must-fix issues. `make verify` green at 145 tests (118 app + 25 engine + 2 CLI, 2 ignored). Decisions on record: row click keeps the popover open; Clear/meta hidden when nothing is upcoming; `remove_at` is total over all indices though the UI only reaches upcoming rows.
- GUI not verifiable in the sandbox. Jason's manual checklist: open/close and press-closed guard on the queue button, jump from the middle, remove a row, Clear, badge correctness throughout, long-queue scroll inside the popover, live time ticking in NOW PLAYING. Next after acceptance: commit/PR per Jason's authorization, then the repo-layer refactor slice, then stage 14.

## 2026-08-10 — Repo-layer store split and schema v4 rebuild

- Queue popover accepted by Jason and merged to `main` as PR #22 first, so this slice starts from a clean mainline instead of carrying the popover edits underneath it. Implemented on `feature/library-repo-layer` (working tree, uncommitted).
- **Store split.** `library/store.rs` (2,955 lines) became a runner-style module family under `library/store/`: `mod.rs` (connection lifecycle — open/pragmas/`genre_has_member` registration/migration dispatch/interrupted-scan recovery — plus the `LibraryStore` facade), `schema.rs` (SCHEMA + migrations), and per-entity `roots.rs`, `tracks.rs`, `albums.rs`, `playlists.rs`, `scans.rs`, `search.rs`. Entity modules expose free functions over `&Connection`/`&Transaction` (runner's repo convention); `LibraryStore` methods own transaction boundaries and compose them — `remove_storage_root` reads as the explicit child-delete sequence it is. The public `LibraryStore` API is unchanged: library_ui, shell, playback_row, and `library/mod.rs` (including its `use store::{...}` imports, via re-exports) compile untouched. Tests moved with their entities; shared helpers live in `store::testing`. New single-definition pins: `TRACK_COLUMNS` (+ a qualified variant for the playlist JOIN) so track SELECTs and the positional mapper can't drift, and `PLAYLIST_SUMMARY_SELECT` shared by the playlist listing and search.
- **Schema v4: the dormant cascades leave.** All four `ON DELETE CASCADE` clauses (tracks/scan_history → storage_roots, playlist_tracks → playlists/tracks) are now plain `REFERENCES` — related-data deletion is application-owned, per the decision recorded in the 2026-08-07 stage-13 entry above (`thread-model.md` carries the thread/process rationale, not this one); the store.rs:321 comment's promised rebuild is this one. SQLite can't edit FK clauses in place, so v3→v4 is the documented table rebuild (create `*_v4`, copy rows preserving ids — cover-cache paths and playlist entries reference track ids — drop, rename, recreate all four indexes) with `PRAGMA foreign_keys` toggled OFF outside the transaction and restored on both exit paths, and `PRAGMA foreign_key_check` gating the commit: violations fail closed with the new `LibraryError::MigrationIntegrity`, rolling back to an intact v3 rather than dropping rows. Fresh databases are created at v4 directly; v1→v2→v3→v4 chains in sequence.
- Migration tests pin the historical v3 schema as a frozen `V3_SCHEMA` const (cascades included — the exact shape live databases have) rather than deriving from the current SCHEMA: v3→v4 rebuild preserving non-contiguous row ids/covers/history plus behavioral cascade-gone proof (direct parent deletes now rejected, app-owned deletes still work), fail-closed on a seeded orphan, fresh-at-v4, version-99 rejection, and the v1/v2 chains updated to land on 4. The explicit-deletion tests (FK off, seeded children) pass unchanged and now prove app-owned deletes with no cascades behind them at all.
- **Dry run on the real library.** Backed up the live `library.sqlite` (v3, 822 tracks, 1 root, clean `foreign_key_check`) via sqlite backup while the app was running, ran the actual migration code on the copy through a temporary env-gated test (removed after): v4, all 822 track ids and cover paths byte-identical, `integrity_check` ok, zero CASCADE clauses left. The next real app launch performs this same migration on the live file.
- Scope notes for review: one new public item (`LibraryError::MigrationIntegrity(String)`, required by the fail-closed rebuild — nothing outside `library` matches `LibraryError` exhaustively); `migrate_v2_to_v3` still creates playlist tables with CASCADE (historical fidelity — v3→v4 strips them moments later); no call-site changes in playback_row/queue/library_ui/shell. No other schema debt surfaced during the rebuild.
- `make verify` green at 149 tests (122 app + 25 engine + 2 CLI, 2 ignored) — up from the 145 baseline, nothing shrank.
- **Review pass clean, no must-fix findings.** Reviewer read the full deleted monolith and all eight replacement modules, checked the facade transaction boundaries plus the callers in `library/mod.rs` and library_ui, and compared the frozen `V3_SCHEMA` fixture against the shipped v3 schema — confirming the v3→v4 rebuild is atomic, preserves named columns/ids/indexes, gates commit on `foreign_key_check`, restores FK enforcement on both exit paths, and that every former cascade has an explicit application delete path. Reviewer's own checks: focused schema tests 6/6, `cargo test -p pulse-app` 122 + 2 ignored, fmt, `git diff --check`, trailing-whitespace scan — all green. Sole finding was a non-blocking doc nit (this entry had attributed the deletion-ownership decision to `thread-model.md`, which doesn't state it; the source is the 2026-08-07 entry), fixed in place above. Jason then authorized commit + PR: committed as fab90fb, PR #23 opened, CI green (Rust / macOS). Merge pending; then stage 14.

## 2026-08-10 — Flat tables, roomier album rows, and the neon playing-row treatment

- Jason-directed design session, Pencil first per the standard flow, then code — all on `feature/flat-tables`, stacked on `feature/library-repo-layer` because PR #23 was still unmerged and both slices touch this log.
- **Three connected UX complaints, resolved in one visual language.** (1) Album-detail track rows were 38px against the tracks page's 58px and Qobuz's ~60px — too thin. (2) Every table sat inside a card (surface fill + 1px border + rounded corners) where Qobuz lays rows directly on the page — the card read as chrome without a job. (3) The playing row stacked four signals (tint, 2px accent left edge, an accent-colored bottom border caused by the single stroke color covering both edges, accent title+icon) — Jason: "two highlight borders … the playing item doesn't layer with the rest."
- **Design (pulse-desktop.pen).** Album-detail rows to 58px. De-carded across every screen: track tables on Tracks/Album Detail and all four variant screens, the playlists list panel (now a right-hairline column divider) and detail panel, the storage roots panel, detail inspector (left hairline), and the storage/playlists empty-state boxes. Headers lost their `bg-surface-alt` band and keep a single bottom hairline. Kept chrome deliberately: album grid cards, storage summary stat tiles, chips, buttons, sidebar, and all floating surfaces (popovers, dialogs). Playing-row treatment chosen from a live canvas exploration of five variants (current, A1 3px bar, A2 glow, A3 brighter tint, plus tint-only and Qobuz-bare from round one): **A2 — 4px accent bar + a left→right neon glow (accent at 15% alpha fading out by the row midpoint) over `bg-selected`, separator back to neutral** — approved on Album Detail, then rolled out to the tracks-page playing rows on all five screens, storage selected roots, playlist selected rows, playlist-preview playing tracks, the search popover's keyboard selection, and the queue popover's NOW PLAYING block (which traded its full accent border for the same bar+glow over `bg-inset`).
- **Code.** New `components::playing_row_glow()` / `playing_row_bar()` — the glow is gpui's two-stop `linear_gradient(90°)` (first gradient use in the app), the bar a 4px absolute-positioned accent rectangle spanning the parent's content box, which in gpui/taffy already excludes the parent's border — so a full vertical span lands exactly against a border_t or border_b separator with no manual inset (review caught the original 1px insets double-counting that). Applied with `.relative()` rows in tracks.rs, albums.rs (row 38→58 included), playlists.rs (both row kinds), storage.rs, shell.rs (all three search renderers), playback_row.rs (NOW PLAYING, plus `overflow_hidden` so the glow respects the rounded corners). De-carding mirrored the design: containers lost `rounded/border_1/bg_surface(_alt)`, headers lost the band (album header gained the `border_b` its rows don't provide; tracks header needs none — row `border_t` supplies the line), playlists list panel `border_r_1`, storage inspector `border_l_1`. Accent-colored separator logic on playing rows deleted in both places it existed (albums.rs, playlists.rs preview rows).
- Jason's first in-app pass caught a pre-existing tracks-page bug the flat layout exposed: the header's TRACK cell is 456px but the row's first cell was 442px, drifting every later column 14px left of its label (invisible enough inside the old card). Row cell widened to 456 to match; album-detail and storage tables were verified aligned already.
- Second pre-existing bug from the same pass: the album header's meta line (`{year} · {N} tracks · {M} min`) had rendered as a bare "…" since it shipped — the div had `min_w_0` + `truncate` but no definite width, so gpui collapsed it to its ellipsis (the Pencil design always showed the real text). Fixed with `flex_1`, giving it the remaining row width beside the flex-none artist name.
- **Review: two must-fix findings, both fixed same session, re-review clean.** (1) The bar helper's 1px top/bottom insets double-counted — gpui/taffy resolves absolute children against the parent's content box, which already excludes the border, so the insets pushed the bar 2px off every separator; now `top_0/bottom_0`. (2) The `Rgba` update literals weren't rustfmt-clean, so `make verify` was failing at the fmt gate while coder's grep-filtered output read as green — process note: verify claims must come from the exit code, not filtered logs. Reviewer independently re-ran `make verify` from the tree and confirmed exit 0.
- `make verify` green at 149 tests (122 app + 25 engine + 2 CLI, 2 ignored) — pure styling, no test changes needed. GUI not verifiable in the sandbox; Jason's manual checklist: play a track and check the glow row on Tracks/Album Detail/playlist preview, the NOW PLAYING block in the queue popover, search-popover keyboard selection, storage root selection, two-pane dividers on Playlists/Storage, header hairlines, pagination footer edge, and that nothing regressed in the empty states.
- Jason authorized the git sequence: PR #23 merged to main (3db3dbd), the slice committed as f0fa328 and opened as PR #24 with CI green (Rust / macOS, 1m7s). Merge pending.

## 2026-08-10 — Delete Album: the ⋯ button earns its place (design + code)

- Follow-on from the flat-tables session: Jason asked whether the album header buttons were wired. Audit: Play Album wired; Shuffle dead (no handler, no shuffle infrastructure anywhere — the transport-bar shuffle icon is dead too); the ⋯ button dead with no menu behind it. Decision: instead of deleting the ⋯ placeholder, give it a real job — an album menu with Delete Album. Shuffle wiring and dead-icon removal noted for a later slice. Implemented on `feature/album-delete`, stacked on `feature/flat-tables` (PR #24 open at the time).
- **Design first (pulse-desktop.pen), approved by Jason before code**: two new screens — "Album Detail — Album Menu" (⋯ opens a 210px context-menu popover, same chrome as the track context menu, one danger-red "Delete Album…" item) and "Album Detail — Delete Album" (confirmation modal on the Add Storage dialog chrome: mini-cover album summary row, a `danger-soft` callout with triangle-alert icon, Cancel + solid-`danger` Delete Album button). Also settled the artist-row mystery: the design's Artist Row always contained the real meta text — only the code collapsed it (fixed in the flat-tables slice).
- **Semantics, confirmed by Jason: delete means files off disk.** The library is scan-based, so a library-only delete would resurrect the album on the next scan — the modal says exactly what happens: "Deletes the album's N audio files from disk and removes its tracks from the library and any playlists. This cannot be undone."
- **Code.** Albums have no id — identity is the `(album_artist|artist, album)` coalescing pair that `tracks_for_album` already implements, so deletion is track-set deletion: new `library::delete_track_files(&[Track])` attempts each unlink and returns (ids whose files are gone, per-file failure messages) — NotFound counts as deleted (the row must go either way), any other error keeps the row so the library never claims a deletion that did not happen. New `LibraryStore::delete_tracks(&[TrackId])` removes rows + playlist entries per track in one transaction (reusing the app-owned `delete_track`). UI: `confirm_delete_album` re-queries the album's tracks at confirm time, deletes files, then rows, then cover-cache files (NotFound-tolerant, mirroring `remove_storage_root`), surfaces a "Deleted N of M files. Could not delete: …" error on partial failure, closes the detail back to the grid, and reloads. Queue entries for deleted tracks are intentionally untouched — the stage-13 missing-file machinery marks and skips them at play time.
- **UI wiring**: the ⋯ button got an id, cursor, accent tint while open, and the artist-picker press-closed guard; the menu is an anchored absolute popover (occlude + `on_mouse_down_out`) with the single danger item; `Modal::DeleteAlbum { album }` renders through the standard modal dispatch with `danger_button` confirm. Menu state resets on open/back/destination-change. New icon assets `trash-2.svg` / `triangle-alert.svg` (lucide, matching the existing stroke format).
- Tests: `delete_tracks_removes_rows_and_playlist_entries_for_only_the_given_tracks` (store: two albums + a mixed playlist — only the doomed album's rows and entries go) and `delete_track_files_tolerates_missing_files_and_keeps_undeletable_tracks` (fs policy: present file deleted, vanished file still counts, a blocked path fails and keeps its track). `make verify` exit 0 at 151 tests (124 app + 25 engine + 2 CLI, 2 ignored) — up from 149.
- **Review round one: six must-fix findings, all fixed.** (1) The unlinks ran synchronously in the click handler — NAS I/O on the UI thread, violating thread-model.md; deletion now runs on a `pulse-album-delete` worker thread through the scan pattern: the store moves into the worker (blocking scans and second deletes), unwinds are caught (`DeleteAlbumPanicked` reopens the library), the modal stays open in a disabled "Deleting…" state, and `DeleteAlbumFinished` restores the store and applies the outcome. (2) NotFound was read as "file gone" unconditionally — an unmounted NAS root ENOENTs every child, so Delete Album would have dropped all rows while the audio survived on the disconnected volume; `delete_track_files` now takes a per-root reachability check and a missing file only counts as deleted while its root is reachable, otherwise the row is kept and the failure says the root is offline. (3) Files were deleted before the DB write with a bare SQLite error on failure — `delete_album_tracks` now preflights the write (`BEGIN IMMEDIATE` acquire/release) before the first unlink, and a post-unlink DB failure reports exactly what happened: "Deleted N of M audio files, but updating the library failed: … Run Delete Album again to finish the cleanup." (4) Deleted rows left runtime missing marks behind with recyclable ids — new `PlaybackRow::remove_missing_marks(&[TrackId])` drops them post-commit, and the now-false "rows are never deleted" comment was rewritten. (5) The modal's copy column repeated the exact `min_w_0`-without-width pattern that broke the album meta line — `flex_1` added. (6) The menu popover was mouse-only — it now tracks the shared focus handle with Escape dismissal per the context-menu precedent, opening focuses the handle, and `album_menu_open` clears on every destination change (not just Albums).
- Orchestration now lives in testable `library::delete_album_tracks(store, artist, title) → DeleteAlbumOutcome` (deleted ids/counts, per-file failures, `db_error`); three new tests beyond the original two: offline-root (root renamed after insertion → all rows kept, failures say offline), forced post-unlink DB failure (via a `break_playlist_entries` testing helper — file gone, row kept, `db_error` set), and the happy path (files, rows, covers all removed).
- `make verify` exit 0 at 154 tests (127 app + 25 engine + 2 CLI, 2 ignored).
- **Review round two: three must-fix + one doc finding, all fixed.** (1) The truthful partial-deletion message was being overwritten when the post-delete reload failed on the same broken database — the report now composes through a testable `delete_album_notice(outcome, reload_error)` that preserves both facts ("Deleted N of M audio files … Reloading the library also failed: …"), with unit tests at the outcome-application boundary. (2) The in-flight modal was dismissible via the shared X/Cancel, and once dismissed, Storage's Rescan hit `store.take().expect(...)` while the delete worker owned the store — a deterministic panic; the modal is now non-dismissible while in flight (header without X, disabled Cancel), storage's action gates block on store absence as well as scans, `start_scan` early-returns with the busy message instead of expecting, and `store_busy_message` names the album delete when that's the owner. (3) Missing marks were removed for unlinked files even when the row commit failed — removal is now gated on `db_error.is_none()`, matching what actually left the library. (4) `thread-model.md`'s thread inventory and data-flow now record the `pulse-album-delete` worker and its store ownership transfer.
- `make verify` exit 0 at 156 tests (129 app + 25 engine + 2 CLI, 2 ignored).
- **Review round three: one medium finding + doc polish, fixed.** The compound path — one file fails to unlink, others succeed, then the DB commit fails — dropped the file-failure list from the notice because `db_error` and `failures` were composed mutually exclusively; `delete_album_notice` now builds from parts so the DB-failure message carries the failure list too (test with both fields non-empty added). The thread-model data-flow connector was reflowed over two aligned lines (`┤`/`┘`) so the ASCII actually connects. `make verify` exit 0 at 157 tests (130 app + 25 engine + 2 CLI, 2 ignored).
- GUI not verifiable in the sandbox; Jason's manual checklist: ⋯ opens/closes the menu (press-closed guard, Escape), Delete Album… opens the modal, Cancel/X dismiss, confirm on a garbage album actually removes the files (verify on disk) with the UI staying responsive, "Deleting…" state visible on a NAS album, grid refreshes, playlists lose the entries, queue skips gracefully if the deleted album was queued, offline-root refusal (eject the NAS and try), partial-failure notice if a file is locked.
- **Review clean after three rounds** (6 + 4 + 1 findings, all fixed with tests). Reviewer's closing verification of the destructive paths: storage I/O off the UI thread; offline roots retain rows; preflight failures unlink nothing; post-unlink DB failures preserve truthful state and missing marks; successful commits remove recyclable-id marks and cover-cache files; the in-flight UI cannot dismiss the operation or start a competing store worker; panic recovery reopens the store. Independent `make verify` from the tree: exit 0.
- Changes uncommitted on `feature/album-delete` pending Jason's in-app pass (delete a garbage album, verify files leave the disk) and commit/PR authorization; PR #24 (flat-tables) merge also pending.

## 2026-08-11 — Feature 04 dev/prod data isolation

- Added one compile-time profile directory name at the existing preferences seam: debug builds resolve both application data and the app output-device preference under `pulse-dev`, while release builds keep the shipped `pulse` paths. No callers, CLI paths, schema behavior, TCC identity, or UI changed.
- Kept the existing filename-shape tests and added debug-build assertions for both resolved profile roots. `make verify` exits 0 at 159 passing tests plus 2 ignored (132 app + 25 engine + 2 CLI), up from the measured 157-test baseline; the focused preference suite passes 5/5.
- Working-tree review found the 2026-08-10 plain directory seed was unsafe: all 809 copied non-null `tracks.cover_art_path` values still pointed at the production `pulse/covers/` cache, so destructive dev actions could remove production covers. Jason chose a clean dev profile instead of rewriting the seed; with Pulse closed, the entire old `pulse-dev` profile was moved temporarily to `/private/tmp/pulse-dev-reset.yi0Y6o/pulse-dev`, leaving no active dev database. macOS may purge that short-lived backup; the lasting safety invariant is that the production directory remained untouched, with its database still at inode `63180010`, size 868352, mtime 2026-08-10 13:44:11. Jason confirmed `make run` now opens the fresh empty dev library. Remaining manual safety checks: scan only disposable dev data, confirm no production cover paths are written, and confirm the bundled release app continues to use the untouched `pulse` library. Issue #26 remains open.

## 2026-08-11 — Tracks infinite scroll and backend pagination

- Updated the Pencil design first: Tracks states `KfJr9`, `f9sxo`, `VSy1Y`, and `LjEHI` now continue the table with a ninth track row instead of a pagination footer, with a slim persistent scrollbar over the scroll region. Structural checks show each region contains only its Tracks table plus scrollbar, no placeholder roots or clipping problems, and the saved `VSy1Y` screenshot has no collapsed or overflowing layout.
- Replaced the Tracks page-number model and footer with the Albums-style near-bottom prefetch loop. The UI keeps a backend-reported total, appends 20-row pages by the loaded-row offset, resets to the top on sort/filter/artist changes, and reloads the already-loaded prefix so scroll position survives library refreshes. A custom 4px scrollbar reflects the loaded range, supports rail clicks and thumb dragging, and stays outside the fixed header without shifting table columns. Playback queue construction remains the existing full matching query, so Next can move beyond rows currently loaded in the viewport.
- Changed a track-page offset at or beyond the result count to return an empty tail instead of snapping backward to the last page, which would duplicate rows during append. Tests now cover contiguous multi-page traversal, total-order stability across tied sort keys, exact/past-end empty pages, and scrollbar geometry at the top/bottom/minimum-thumb boundaries; the obsolete page-control model tests were removed. `make verify` exits 0 at 160 passing tests plus 2 ignored (133 app + 25 engine + 2 CLI), up from the 159-test branch baseline; focused track-page tests pass 3/3 and the scrollbar-metrics test passes 1/1.
- Updated the manual checklist for infinite scrolling and Pencil node `VSy1Y`; Jason reported the final `make run` smoke test passed after the scrollbar fixes. All working-tree review rounds found no must-fix issues; all suggestions were applied, including a zero-travel guard that prevents a sub-pixel overflow from writing a NaN scroll offset, using the live scroll handle in callbacks, and explicit grab/rail-extreme checks. Changes remain uncommitted pending the authorized PR workflow.

## 2026-08-12 — Feature 06 settings surface

- Implemented the Pencil-defined full-window Settings destination with its 236px section sidebar, General/Update/About routes, library-return path, sidebar and `Cmd+,` entry points, centered 820px content column, shared card/row styling, and the new on/off toggle component. The existing playback entity stays alive while Settings hides the transport, then returns intact; General reuses that entity's output-device popover and resolves the active device name. Update is deliberately idle presentation only. About links use GPUI's macOS URL opener, which delegates to `NSWorkspace`; the design's unsupported bit-perfect tagline was replaced with Jason's approved “Native-rate music player for macOS.”
- Added one global exclusive-mode preference beside the output-device preference, defaulting on when absent. The setting now reaches the controller/backend factory and gates only `HogGuard::acquire`: on preserves today's hog request, off skips it. A live change restarts active playback at its logical position under the requested mode; paused and idle playback use it on the next backend open. The CLI explicitly keeps exclusive mode on.
- Added view-model/component/preference/controller coverage for section selection, active-device-name resolution, exact toggle appearances, absent-file default plus preference round-trip, and live backend reopen without losing position. `make verify` exits 0 at 165 passing tests plus 2 ignored (137 app + 26 engine + 2 CLI), up from the measured 160-test baseline. Working-tree review is clean with no must-fix issues; Jason confirmed the corrected layout, with behavioral acceptance remaining manual.
- Jason's first GUI passes caught two layout mismatches: the design's 24px Settings sidebar top padding placed Back to library in the native titlebar traffic-light band, and General's PLAYBACK group was the only settings content missing the 820px maximum width. Settings now uses the shell's 54px native-titlebar offset, and every page title and group shares the same centered 820px column.

## 2026-08-13 — Feature 05 update check

- Added a strict pure `vMAJOR.MINOR.PATCH` comparison and fixture-tested GitHub response parser, plus one blocking `ureq` GET on GPUI's background executor with a five-second global timeout, explicit Pulse user agent, redirects disabled, and no retry. Invalid, prerelease, suffixed, older, and equal tags do not notify.
- Wired the Pencil-defined IDLE/CHECKING/AVAILABLE/FAILED Update states, release-page actions, the Pulse ▸ Check for Updates… menu path, and the per-launch playback notice banner. Automatic checks wait until after first paint; automatic failures return silently to IDLE while manual failures render the warning state, and request revisions prevent an older completion from overwriting a newer manual check.
- Persisted the launch opt-out as `check-updates.disabled` beside the existing marker-file preference, defaulting on when absent; manual checks remain available while opted out. README now records the exact unauthenticated GitHub request and that it sends no identifiers.
- Working-tree review found one design mismatch: CHECKING reused `refresh-cw` instead of the Pencil board's Lucide `loader`. Added and registered the matching loader asset, reran the full checks, and re-review is clean with no remaining must-fix issues; the duplicated marker helpers stay explicit under the project's no-premature-abstraction rule.
- `make verify` exits 0 at 175 passing tests plus 2 ignored (147 app + 26 engine + 2 CLI), up from the measured 165 passing plus 2 ignored baseline. Jason's GUI/network checks are next; no code blocker is known.
